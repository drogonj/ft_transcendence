services:
  postgres:
    container_name: postgres
    image: postgres:16.3
    volumes:
      - ./containers/volumes/postgres:/var/lib/postgresql/data/
    env_file:
      - ./containers/.env
    ports:
      - '5432:5432'
    networks:
      - network
  django:
    container_name: django
    build: ./containers/django/
    volumes:
      - ./containers/volumes/django:/backend/
    ports:
      - "8000:8000"
    env_file:
      - ./containers/.env
    depends_on:
      - postgres
    networks:
      - network
  nginx:
    container_name: nginx
    build: containers/nginx
    ports:
      - "8080:443"
    restart: always
    depends_on:
      - django
    networks:
      - network

networks:
  network:
    driver: bridge